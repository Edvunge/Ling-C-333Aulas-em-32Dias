#include <stdio.h>

int main(void) {
    int x = 8, y = 16;

    // Do somenthing if x less than 10 and y greater than 20:
    if (x < 10 && y > 20)
        printf("Doing Something!\n");

    if (!(x < 12))
        printf("x is not less than 12\n");

    if (x >= 12)
        printf("x is not less than 12\n");

    printf("sai do programa...");            
    return (0);
}