#include <stdio.h>
#include <stdlib.h>

int main(void) {
    // declarando um vetor
    int idade[10];

    // atribuiçao
    idade[0] = 15;
    idade[4] = 40;

    // acesso
    x = idade[4];
    if(idade[2]<12) printf("crianca");
    return (0);
}