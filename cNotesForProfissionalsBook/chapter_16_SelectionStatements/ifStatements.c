#include <stdio.h>

int main(void) {

    if (cond) {
        statements(s);
    }


    if (a > 1) {
        puts("a is larger than 1");
    }

    if (a > 1)
        puts("a is larger than 1");

    
    if ((a > 1) && (b > 1)) {
        puts("a is larger than 1");
    }

    return (0);
}