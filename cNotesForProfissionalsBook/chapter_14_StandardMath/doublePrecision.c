#include <math.h>
#include <stdio.h>

doble fmod(double a, double b);

int main(void) {

    double x = 10.0;
    double y = 5.1;

    double modulus = fmod(x, y);
    printf("%lf\n", modulus);

    return (0);
}


doble fmod(double a, double b) {
    if (a > 0) {
        
    }
    return (a  b);
}