#include <stdio.h>
#include <stdlib.h>

/*
11. Faca um programa que leia um numero inteiro positivo N 
e imprima todos os numeros naturais de 0 ate N em ordem crescente.
*/

int main(void) {
    int t = 0, numN;

    for(t; t < numN; t++) {
        printf(" %d ",t);        
    }
    return (0);
}