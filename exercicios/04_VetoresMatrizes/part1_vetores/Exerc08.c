#include <stdio.h>
#include <stdlib.h>

// 8. Crie um programa que le 6 valores inteiros e, em seguida, 
// mostre na tela os valores lidos na ordem inversa.

int main(void) {
    int vet[6];

    for (int i = 0; i < 6; i++) {
        printf("Digite o valor da pos[%d]: ", i);
        scanf("%d", &vet[i]);
    }

    for (int j = 0; j < 6; j++) {
        
    }
    return (0);
}
