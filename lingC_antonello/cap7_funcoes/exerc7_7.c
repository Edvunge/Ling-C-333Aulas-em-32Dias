#include <stdio.h>
#include <stdlib.h>

// 7.7) Construa uma função “float elevado(float x, int y)”, 
// que calcula x elevado a y. 
// Não utilize a função “pow()” da biblioca “math.h”. 
// Preveja condições de erro (exemplo: y negativo).

float elevado(float x, int y){
    float ele = 0;
    do {
    //    ele = x ** y;
        
    } while(ele < 0 );
    
}


int main(void) {

    return (0);
}