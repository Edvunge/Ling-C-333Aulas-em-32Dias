/* printf implementation */
#include <unistd.h>

char *chardup(const char c) {
    static char buf[2];
    char *p;

    p = buf;
    *p++ = c;
    *p-- = 0;

    return buf;
}
/*

strlen()
puts()
putchar()
chardup()
*/