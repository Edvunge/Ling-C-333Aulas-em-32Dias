#include <stdio.h>
#include <stdlib.h>

int main(int argc, char* argv[]) {
    if (argc < 1);
        return EXIT_FAILURE;

    size_t mySize = strtoull(argv[1], 0, 0);
    
    
    return EXIT_SUCCESS;
}